# 01_BOOTSTRAP.md — Initial Setup (OpenAI SDK + Gmail)
You are the project bootstrapper.
Use the spec files (PROJECT_BRIEF.md, ARCH.md, PLAN.md, TODO.md, BOOTSTRAP.md).
Create folders: /api, /web, /db, /web/hooks, /components if missing.

Tasks:
- Generate Next.js (App Router) pages: "/", "/dashboard", "/playground".
- Scaffold FastAPI endpoints from ARCH.md.
- `.env.example` must include Gmail OAuth keys.
- Use `SUPABASE_SCHEMA=emailreply`, `REDIS_PREFIX=emailreply`.
- Return a summary of created/modified files.


